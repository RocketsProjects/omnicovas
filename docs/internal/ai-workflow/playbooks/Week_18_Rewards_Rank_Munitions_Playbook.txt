================================================================================
WEEK 18 PLAYBOOK — Rewards, Combat Rank, and Munitions
================================================================================

OPERATING ASSUMPTIONS FOR SOLDIER

You are executing this Playbook under Soldier.md.

Rules:
  1. Execute only the edits listed here.
  2. Touch only the allowed files.
  3. Do not explore the repository or search for related files.
  4. Do not add dependencies.
  5. Do not invent journal fixtures, KB values, thresholds, commander records,
     ship stats, rank mappings, or external API behavior.
  6. If a required file, fixture, or source is missing, output:
     ESCALATE: <one-sentence reason>
  7. End with the exact six-section Soldier output format.

COMMANDER INTENT

Implement telemetry-driven bounty tracking, combat bond tracking, combat rank tracking, and munitions snapshots. Do not infer missing ammo. Do not implement CZ, AX, on-foot, or advanced debrief features.

REQUIRED TAGGED FILES

  - Soldier.md
  - CLAUDE.MD
  - OmniCOVAS_Phase4_Development_Guide.txt
  - omnicovas/core/event_types.py
  - omnicovas/core/state.py
  - omnicovas/core/dispatcher.py
  - omnicovas/core/broadcaster.py
  - omnicovas/features/combat/__init__.py
  - omnicovas/api/combat.py
  - Existing combat UI/dashboard files
  - Architect-supplied bounty and combat bond journal fixtures
  - Architect-supplied combat rank fixture, if rank tracking is expected
  - Architect-supplied munitions/ammo telemetry fixture, if munitions tracking is expected
  - KB source entries for rank mapping or ammo thresholds, if used

ALLOWED FILES TO CREATE

  - omnicovas/features/combat/rewards.py
  - omnicovas/features/combat/rank.py
  - omnicovas/features/combat/munitions.py
  - tests/test_combat_rewards.py
  - tests/test_combat_rank.py
  - tests/test_combat_munitions.py

ALLOWED FILES TO MODIFY

  - omnicovas/core/state.py
  - omnicovas/core/dispatcher.py
  - omnicovas/features/combat/__init__.py
  - omnicovas/api/combat.py
  - Existing combat UI/dashboard files

EDIT STEPS

1. Create rewards.py and CombatReward fields: reward_type, amount, faction, target, voucher_type, timestamp, source_event.
2. Implement handlers only for reward event names and fields present in attached bounty/bond fixtures.
3. Track bounty_session_total and combat_bond_session_total separately.
4. Publish BOUNTY_AWARDED and COMBAT_BOND_AWARDED with amount, type, optional faction/target, timestamp, and source.
5. Create rank.py. Capture combat rank only from attached telemetry. Unknown remains None.
6. Do not create rank order or rank mapping unless KB/source entries are attached.
7. Publish COMBAT_RANK_CHANGED when a real rank transition occurs.
8. Create munitions.py and MunitionsSnapshot fields: slot, item, ammo_clip, ammo_hopper, synthesized_capacity, updated_at, source_refs.
9. Implement munitions updates only from attached telemetry. Unknown ammo remains None, not zero.
10. Publish MUNITIONS_CHANGED only when munitions state changes.
11. Add GET /combat/rewards, GET /combat/rank, and GET /combat/munitions to combat.py.
12. Add UI cards for Rewards, Rank, and Munitions. Use WebSocket first and endpoint fallback second.

TESTS REQUIRED

1. tests/test_combat_rewards.py covers bounty, bond, separate totals, duplicate replay if idempotency pattern exists, and endpoint shape.
2. tests/test_combat_rank.py covers unknown initial rank, transition if fixture supports it, and broadcast.
3. tests/test_combat_munitions.py covers nullable unknown ammo, known ammo from fixture, broadcast, and endpoint shape.

VERIFICATION COMMANDS

  ruff check omnicovas/
  mypy omnicovas/
  pytest tests/test_combat_rewards.py tests/test_combat_rank.py tests/test_combat_munitions.py
  pytest
  npm run tauri build

ESCALATE IF

  - Reward fixtures are missing.
  - Rank tracking is expected but no rank fixture is attached.
  - Munitions tracking is expected but no munitions fixture is attached.
  - A requested rank mapping or ammo threshold lacks KB/source entries.
  - Existing idempotency pattern is not attached but duplicate replay behavior is required.
  - Any test fails after the requested edits.

END OF PLAYBOOK
