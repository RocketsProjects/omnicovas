OmniCOVAS PB05 v2 — Command Deck + Route-Specific Console UI
Executor Target: Google Gemini CLI / lightweight executor
Architect Authority: CLAUDE.MD + OmniCOVAS_Index.md + Master Blueprint v4.2
Standing Executor Alignment: GEMINI.md + CLAUDE_CODE.md
Safety Authority: ui_safe_rendering_checklist.md

GLOBAL PB05 v2 CONSTRAINTS
- Do not add dependencies unless the playbook explicitly opens a dependency gate. Default: no dependencies.
- Do not redesign backend FastAPI contracts unless explicitly allowed. Default: no backend contract changes.
- Do not build Phase 4 combat logic.
- Do not invent telemetry. Unknown, missing, unsupported, or unreliable telemetry stays truthful.
- Preserve Tauri/FastAPI dynamic bridge behavior.
- Preserve static vanilla Tauri UI architecture. No framework migration.
- Preserve classic script loading unless the playbook explicitly proves an existing pattern is safe.
- Use safe DOM rendering. Dynamic/API/user data uses textContent, createElement, replaceChildren, setAttribute.
- No unsafe dynamic innerHTML/outerHTML/insertAdjacentHTML.
- No Frontier assets, screenshots, traced silhouettes, extracted game models, or copied ship art.
- Ship schematics must be original internal SVG vector technical line art.
- Cool visuals are allowed only when they make the app easier to understand.

LOCKED PB05 v2 UX MODEL
- Left sidebar remains primary navigation.
- Sidebar labels remain simple: Dashboard, Activity Log, Resources, Settings, Privacy, Credits, Future Systems.
- Each route opens its own dedicated page.
- Dashboard is the only page with the ship wireframe / ship systems schematic hero.
- Other pages get route-specific identities but remain straightforward:
  Dashboard     -> Ship Systems Schematic
  Activity Log  -> Flight Recorder / Black Box
  Resources     -> Diagnostics Console
  Settings      -> Configuration Bay
  Privacy       -> Firewall / Data Control
  Onboarding    -> Commissioning Flow
  Overlay       -> HUD Projection
  Credits       -> Registry Plaque
- Dashboard schematic: top-down blueprint.
- First ship schematic: Sidewinder, recognizable but simplified original approximation.
- Generic fallback schematic is required.
- Dashboard panels: visible by default, hide/show from accessible hotspots.
- Panel hidden/shown state resets every launch for PB05.
- Future compact dashboard mode is architected for later, not implemented in PB05.
- Free-drag floating windows are forbidden in PB05. Snap zones are future optional work, not initial schematic MVP.

STANDARD VERIFICATION SET
Run playbook-specific commands first. Common commands:
npm test
git grep -n "innerHTML\|outerHTML\|insertAdjacentHTML" -- ui
git grep -n "8000\|OMNICOVAS_PORT || 8000\|window.PORT || 8000" -- ui
git diff -- package.json package-lock.json
git status

MANUAL SMOKE RULE
Do not claim manual smoke passed unless you personally ran it or Commander reported it passed.

# PB05-08 — Settings Configuration Bay, Overlay Bridge, and Banner Test Center

OBJECTIVE
Implement Settings as a clear Configuration Bay with accessible front-end tabs and bridge existing Overlay settings into Settings. Add Banner Test Center so the Commander can test overlay banners without DevTools.

ALLOWED FILES
Modify:
- ui/index.html
- ui/views/settings.js
- ui/overlay.js
- ui/overlay.html, only if needed for compatible event/class handling
- ui/styles/settings.css, create if missing
- ui/styles/forms.css
- ui/styles/pages.css
- ui/styles/components.css
- ui/styles/overlay.css, create if needed
- ui/__tests__/settings.test.js
- ui/__tests__/overlay.test.js

Modify only if selected/all banner testing requires narrow Tauri command:
- src-tauri/src/lib.rs
- tests/test_tauri_autoconnect_bridge.py

FORBIDDEN
No Python backend changes unless existing overlay endpoint is objectively broken; stop first. No package/Cargo dependency changes. No fake telemetry. No combat logic. No AI provider implementation. No broad overlay behavior rewrite.

SETTINGS TABS
Accessible tabs:
Basic, Overlay, AI / Advisory, Future Systems, Advanced.
Sidebar label stays Settings. Page header can say Configuration Bay. Keep controls conventional.

TAB CONTENT
Basic: preset profiles / operating profile.
Overlay: opacity, anchor, event toggles, click-through state if available, banner test center.
AI / Advisory: provider/API key placeholder and Confirmation Gate sovereignty copy.
Future Systems: reserved pillar toggles/modules.
Advanced: save/reset/export/import, diagnostics if already present.

OVERLAY SETTINGS BRIDGE
Use window.Shell.httpBase or window.OMNICOVAS_PORT only. No fixed :8000 fallback.
Use GET/POST /pillar1/overlay/settings.
Preserve /week13/settings behavior.

BANNER TEST CENTER
Controls:
- Test generic banner
- Select banner type
- Test selected banner
- Test all banners
Known event types: HULL_CRITICAL_10, SHIELDS_DOWN, HULL_CRITICAL_25, FUEL_CRITICAL, MODULE_CRITICAL, FUEL_LOW, HEAT_WARNING, HEAT_DAMAGE, OMNICOVAS_TEST.
Testing banners must not mutate Python state or telemetry.
If existing Tauri command cannot test selected/all banners, add the smallest compatible command/event in lib.rs only. No dependencies.

SAFE RENDERING
Banner labels/values and hostile test strings render as text.

TESTS
Tabs switch accessibly. Existing Settings paths still guarded by apiUrl. Overlay settings section renders. Event select contains only known event types. Test controls exist. Hostile banner value safe-rendered. Backend unchanged.

VERIFICATION
npm test
pytest tests/test_tauri_autoconnect_bridge.py -v
git grep -n "innerHTML\|outerHTML\|insertAdjacentHTML" -- ui
cargo check --manifest-path src-tauri/Cargo.toml, if Rust changed
git diff -- omnicovas/api/pillar1.py omnicovas/api/week13.py package.json package-lock.json Cargo.toml Cargo.lock
git status

MANUAL SMOKE
npm run tauri dev
Confirm Settings tabs, Overlay settings load/save, banner tests, clean bridge behavior, no red devtools errors.

COPY/PASTE GEMINI PROMPT
You are Gemini CLI acting as OmniCOVAS executor. Execute PB05-08 exactly from this file. Build Settings tabs and overlay banner test center only. Preserve backend contracts. Do not fake telemetry. Add only the narrowest Tauri command if necessary and no dependencies.
